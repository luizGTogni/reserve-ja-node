export interface Booking {
  id: string;
}

// id, user_id, property_id, data_checkin, data_checkout, número de hospedes,
// valor_base, valor_hospede, valor_host, platform_fee, tax_value,
// status (pendente, confirmado, cancelado, concluído).

// payments
// id, booking_id, valor,
// status (retido, liberado, estornado), metodo_pagamento.
